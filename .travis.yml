language: python
python:
    - "3.6"
install:
    - pip install -r requirements.txt
script:
    #- dvc init
    #- dvc remote add -d s3Remote s3://mybucket/myproject
    #- dvc pull
    #- dvc repro $(dvc root)/dvc_files/Dvcfile
    - pytest